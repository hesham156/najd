# إنشاء صفحة إدارة المستخدمين ✅

## 🐛 المشكلة

عند محاولة الوصول إلى صفحة المستخدمين:

```
GET http://localhost:3000/users 404 (Not Found)
```

### السبب:

الصفحة كانت **مشار إليها** في:
- ✅ `Navbar.tsx` - رابط "المستخدمين" للمدير
- ✅ `dashboard/page.tsx` - زر "إدارة المستخدمين"

**لكن الصفحة نفسها غير موجودة!** ❌

## ✅ الحل

تم إنشاء صفحة كاملة لإدارة المستخدمين: `apps/web/src/app/users/page.tsx`

## 🎨 مميزات الصفحة

### 📊 إحصائيات شاملة

في أعلى الصفحة:
- **إجمالي المستخدمين** - العدد الكلي
- **نشط** - المستخدمون النشطون
- **غير نشط** - المستخدمون المعطلون

### 🔍 بحث وتصفية متقدم

ثلاث طرق للتصفية:
1. **البحث النصي** - بالاسم، البريد، أو رقم الهاتف
2. **القسم** - عرض مستخدمي قسم معين
3. **الدور** - عرض دور وظيفي معين

### 📋 جدول المستخدمين

يعرض:
- **الاسم** - مع صورة دائرية بالحرف الأول
- **البريد الإلكتروني**
- **رقم الهاتف**
- **القسم** - بشارة ملونة
- **الدور الوظيفي**
- **الحالة** - نشط/غير نشط
- **تاريخ الإنشاء**

### 👑 تمييز رؤساء الأقسام

المستخدمون الذين `isHead: true` يظهر بجانب اسمهم:
```
👑 رئيس قسم
```

### 📈 إحصائيات الأقسام

في نهاية الصفحة، عرض توزيع المستخدمين على الأقسام:
```
┌──────────┬──────────┬──────────┐
│ المبيعات │ التصميم  │ الطباعة  │
│    5     │    3     │    2     │
└──────────┴──────────┴──────────┘
```

### ➕ إضافة مستخدم جديد

زر "إضافة مستخدم" يفتح:
```
/scripts/create-user.html
```
في نافذة جديدة لإنشاء مستخدم بسهولة.

## 🔒 الأمان

### صلاحيات الوصول

```typescript
if (user.role !== 'ceo') {
  toast.error('ليس لديك صلاحية لعرض هذه الصفحة');
  router.push('/dashboard');
  return;
}
```

- ✅ **فقط CEO** يمكنه الوصول للصفحة
- ❌ أي دور آخر يتم إعادة توجيهه للوحة التحكم
- 🔔 رسالة خطأ واضحة

## 📱 التصميم المتجاوب

### Desktop (شاشات كبيرة)
```
┌────────────────────────────────────┐
│  الإحصائيات (3 أعمدة)              │
├────────────────────────────────────┤
│  البحث والتصفية (3 أعمدة)          │
├────────────────────────────────────┤
│  جدول المستخدمين (عرض كامل)        │
├────────────────────────────────────┤
│  إحصائيات الأقسام (6 أعمدة)        │
└────────────────────────────────────┘
```

### Mobile (هواتف)
```
┌──────────────┐
│ الإحصائيات   │
│   (عمود 1)   │
├──────────────┤
│   البحث      │
│   (عمود 1)   │
├──────────────┤
│    جدول      │
│   (scroll)   │
├──────────────┤
│  إحصائيات    │
│   (عمود 2)   │
└──────────────┘
```

## 🎯 التكامل مع Firestore

### قراءة المستخدمين

```typescript
const q = query(
  collection(db, COLLECTIONS.USERS), 
  orderBy('createdAt', 'desc')
);
const querySnapshot = await getDocs(q);
```

- يقرأ جميع المستخدمين
- مرتبون من الأحدث إلى الأقدم
- يعرض رسالة خطأ إذا فشل التحميل

### البيانات المعروضة

```typescript
interface User {
  uid: string;
  email: string;
  displayName: string;
  phoneNumber?: string;
  role: string;
  department: string;
  isHead: boolean;
  isActive: boolean;
  createdAt: any;
  updatedAt: any;
}
```

## 🏷️ التسميات العربية

### الأدوار الوظيفية

```typescript
const ROLE_LABELS = {
  ceo: 'المدير التنفيذي',
  sales: 'موظف مبيعات',
  sales_head: 'مدير المبيعات',
  design: 'مصمم',
  design_head: 'مدير التصميم',
  printing: 'موظف طباعة',
  printing_head: 'مدير الطباعة',
  accounting: 'محاسب',
  accounting_head: 'مدير الحسابات',
  dispatch: 'موظف إرسال',
  dispatch_head: 'مدير الإرسال',
};
```

### الأقسام

```typescript
const DEPARTMENT_LABELS = {
  management: 'الإدارة',
  sales: 'المبيعات',
  design: 'التصميم',
  printing: 'الطباعة',
  accounting: 'الحسابات',
  dispatch: 'الإرسال',
};
```

## 🧪 الاختبار

### الخطوات:

1. **سجل الدخول كـ CEO:**
   - البريد: `ceo@najd.com`
   - كلمة المرور: `Najd@2024`

2. **اذهب إلى صفحة المستخدمين:**
   ```
   http://localhost:3000/users
   ```
   أو انقر على "المستخدمين" في Navbar

3. **جرب الميزات:**
   - ✅ شاهد الإحصائيات
   - ✅ ابحث عن مستخدم
   - ✅ صفي حسب القسم
   - ✅ صفي حسب الدور
   - ✅ انقر "إضافة مستخدم"

### النتيجة المتوقعة ✅

```
✓ الصفحة تظهر بشكل كامل
✓ جدول المستخدمين معروض
✓ الإحصائيات صحيحة
✓ البحث والتصفية يعملان
✓ لا يوجد خطأ 404
```

## 🔮 التحسينات المستقبلية

يمكن إضافة:

### 1. تعديل المستخدم
- زر "تعديل" في كل صف
- نموذج لتعديل البيانات
- تحديث الدور والقسم

### 2. تعطيل/تفعيل المستخدم
- زر toggle للحالة النشطة
- تأكيد قبل التعطيل

### 3. حذف المستخدم
- زر حذف (بتأكيد)
- حذف من Authentication و Firestore

### 4. عرض تفاصيل المستخدم
- صفحة `/users/[id]`
- سجل نشاطات المستخدم
- الطلبات التي أنشأها

### 5. إنشاء مستخدم مباشر
- نموذج في الصفحة نفسها
- بدلاً من فتح نافذة جديدة

### 6. تصدير البيانات
- تصدير قائمة المستخدمين كـ Excel
- أو PDF

### 7. إرسال إشعار جماعي
- إرسال إشعار لكل المستخدمين
- أو لقسم معين

## 📊 الإحصائيات المتاحة

| الإحصائية | الوصف |
|-----------|--------|
| **إجمالي المستخدمين** | العدد الكلي |
| **نشط** | عدد المستخدمين النشطين |
| **غير نشط** | عدد المستخدمين المعطلين |
| **توزيع الأقسام** | عدد المستخدمين في كل قسم |

## 🎨 الألوان والتصميم

### بطاقات الإحصائيات
- خلفية بيضاء
- ظل خفيف
- أيقونات كبيرة

### الجدول
- خلفية بيضاء
- خطوط فاصلة رمادية
- hover effect

### الشارات (Badges)
- **القسم**: أزرق فاتح
- **نشط**: أخضر
- **غير نشط**: أحمر
- **رئيس قسم**: ذهبي

## 📚 الملفات المتأثرة

### تم إنشاؤها:
- ✅ `apps/web/src/app/users/page.tsx` - صفحة إدارة المستخدمين

### موجودة مسبقاً ومتصلة:
- `apps/web/src/components/Layout/Navbar.tsx` - رابط المستخدمين
- `apps/web/src/app/dashboard/page.tsx` - زر إدارة المستخدمين
- `scripts/create-user.html` - صفحة إنشاء المستخدم

## ✅ الملخص

| العنصر | الحالة |
|--------|--------|
| صفحة المستخدمين | ✅ تم إنشاؤها |
| عرض قائمة المستخدمين | ✅ يعمل |
| البحث والتصفية | ✅ يعمل |
| الإحصائيات | ✅ تعمل |
| صلاحيات CEO فقط | ✅ محمية |
| تصميم متجاوب | ✅ جاهز |

## 🚀 جاهز للاستخدام!

الآن المدير (CEO) يمكنه:
- ✅ عرض جميع المستخدمين
- ✅ البحث عن مستخدم معين
- ✅ التصفية حسب القسم أو الدور
- ✅ مشاهدة الإحصائيات
- ✅ إضافة مستخدمين جدد
- ✅ معرفة رؤساء الأقسام

**صفحة إدارة المستخدمين جاهزة بالكامل!** 🎉✨

---

© 2024 شركة نجد - جميع الحقوق محفوظة


