# 💰 قسم الحسابات - Accounting Department

## 🎯 نظرة عامة

**قسم الحسابات** هو القسم المسؤول عن إدارة الأمور المالية في شركة نجد، ويتخصص في:

- ✅ **إنشاء عروض الأسعار (Quotations)** للعملاء
- ✅ **إصدار الفواتير (Invoices)** 
- ✅ **إدارة المدفوعات والمستحقات**
- ✅ **متابعة الفواتير المتأخرة**
- ✅ **إعداد التقارير المالية**

---

## 👥 الأدوار والصلاحيات

### 1. محاسب (Accounting)
**الدور:** `accounting`
**القسم:** `accounting`

#### الصلاحيات:
```typescript
{
  canReviewOrder: true,              // مراجعة الطلبات
  canApproveOrder: true,             // الموافقة على الطلبات
  canViewDepartmentOrders: true,     // عرض طلبات القسم
  
  // صلاحيات عروض الأسعار
  canCreateQuotation: true,          // إنشاء عرض سعر
  canEditQuotation: true,            // تعديل عرض سعر
  canApproveQuotation: true,         // الموافقة على عرض سعر
  
  // صلاحيات الفواتير
  canCreateInvoice: true,            // إنشاء فاتورة
  canEditInvoice: true,              // تعديل فاتورة
  canManagePayments: true,           // إدارة المدفوعات
}
```

### 2. مدير الحسابات (Accounting Head)
**الدور:** `accounting_head`
**القسم:** `accounting`

#### الصلاحيات الإضافية:
```typescript
{
  ...صلاحيات المحاسب,
  canViewAllOrders: true,            // عرض جميع الطلبات
  canGenerateReports: true,          // إنشاء التقارير
}
```

---

## 📋 عروض الأسعار (Quotations)

### ما هو عرض السعر؟

عرض السعر هو مستند رسمي يُرسل للعميل يحتوي على:
- تفاصيل المنتجات/الخدمات المطلوبة
- الأسعار التفصيلية
- شروط الدفع والتسليم
- تاريخ صلاحية العرض
- الشروط والأحكام

### دورة حياة عرض السعر

```
1. مسودة (Draft)
   ↓
2. في انتظار الموافقة الداخلية (Pending Approval)
   ↓
3. تمت الموافقة (Approved)
   ↓
4. تم إرساله للعميل (Sent to Client)
   ↓
5. العميل يراجع (Client Reviewing)
   ↓
6a. العميل وافق (Client Accepted) → تحويل لطلب
6b. العميل رفض (Client Rejected)
6c. قيد التفاوض (Negotiating)
```

### حالات عرض السعر

| الحالة | الوصف | الإجراء التالي |
|--------|-------|----------------|
| `quotation_draft` | مسودة | إرسال للموافقة |
| `quotation_pending_approval` | في انتظار الموافقة | الموافقة/الرفض |
| `quotation_approved` | تمت الموافقة | إرسال للعميل |
| `quotation_sent` | تم إرساله للعميل | تسجيل رد العميل |
| `quotation_client_reviewing` | العميل يراجع | تسجيل القبول/الرفض |
| `quotation_accepted` | العميل وافق | تحويل لطلب |
| `quotation_rejected` | العميل رفض | إغلاق |
| `quotation_negotiating` | قيد التفاوض | تحديث السعر |
| `quotation_converted` | تم تحويله لطلب | - |
| `quotation_expired` | منتهي الصلاحية | - |

### بنية عرض السعر

```typescript
interface Quotation {
  id: string;
  quotationNumber: string;        // QT-2025-0001
  status: QuotationStatus;
  
  // معلومات العميل
  customerName: string;
  customerPhone: string;
  customerEmail?: string;
  companyName?: string;
  taxNumber?: string;             // الرقم الضريبي
  
  // بنود عرض السعر
  items: QuotationItem[];
  
  // المعلومات المالية
  subtotal: number;               // المجموع الفرعي
  taxRate: number;                // 15% في السعودية
  taxAmount: number;              // قيمة الضريبة
  discount?: number;              // الخصم
  totalAmount: number;            // المبلغ الإجمالي
  
  // شروط الدفع
  paymentTerms: PaymentTerms;
  
  // التواريخ
  issueDate: string;              // تاريخ الإصدار
  validUntil: string;             // صالح حتى
  expiryDate: string;             // تاريخ انتهاء الصلاحية
}
```

---

## 🧾 الفواتير (Invoices)

### ما هي الفاتورة؟

الفاتورة هي مستند رسمي يُصدر بعد تنفيذ الطلب، يحتوي على:
- تفاصيل المنتجات/الخدمات المنفذة
- الأسعار النهائية
- الضريبة المضافة (15%)
- المبلغ الإجمالي المستحق
- شروط الدفع والاستحقاق

### أنواع الفواتير

| النوع | الوصف |
|------|-------|
| `sales` | فاتورة مبيعات عادية |
| `proforma` | فاتورة أولية (قبل التنفيذ) |
| `credit_note` | إشعار دائن (استرداد/تخفيض) |
| `debit_note` | إشعار مدين (رسوم إضافية) |

### حالات الفاتورة

| الحالة | الوصف | الإجراء التالي |
|--------|-------|----------------|
| `invoice_draft` | مسودة | إرسال للموافقة |
| `invoice_pending_approval` | في انتظار الموافقة | الموافقة/الرفض |
| `invoice_approved` | تمت الموافقة | إرسال للعميل |
| `invoice_sent` | تم إرسالها للعميل | تسجيل دفعة |
| `invoice_partially_paid` | مدفوعة جزئياً | تسجيل الدفعة المتبقية |
| `invoice_fully_paid` | مدفوعة بالكامل | إغلاق |
| `invoice_overdue` | متأخرة | إرسال تذكير |
| `invoice_cancelled` | ملغاة | - |
| `invoice_refunded` | مستردة | - |

### بنية الفاتورة

```typescript
interface Invoice {
  id: string;
  invoiceNumber: string;          // INV-2025-0001
  invoiceType: InvoiceType;
  status: InvoiceStatus;
  
  // معلومات العميل
  customerName: string;
  customerPhone: string;
  companyName?: string;
  taxNumber?: string;
  commercialRegister?: string;
  
  // الربط مع الطلبات وعروض الأسعار
  relatedOrderId?: string;
  relatedQuotationId?: string;
  
  // بنود الفاتورة
  items: InvoiceItem[];
  
  // المعلومات المالية
  subtotal: number;
  taxAmount: number;
  totalAmount: number;
  paidAmount: number;             // المبلغ المدفوع
  remainingAmount: number;        // المبلغ المتبقي
  
  // سجلات الدفعات
  paymentRecords: PaymentRecord[];
  
  // التواريخ
  issueDate: string;
  dueDate?: string;               // تاريخ الاستحقاق
  paidDate?: string;              // تاريخ الدفع الكامل
}
```

### سجل الدفعة

```typescript
interface PaymentRecord {
  id: string;
  amount: number;                 // المبلغ المدفوع
  paymentMethod: string;          // نقدي، بنكي، شيك، إلخ
  paymentDate: string;
  reference?: string;             // رقم الشيك/التحويل
  receivedBy: string;
  receivedByName: string;
  receiptNumber?: string;         // رقم سند القبض
}
```

---

## 🔄 سير العمل (Workflow)

### 1. من طلب عرض سعر إلى فاتورة

```
العميل يطلب عرض سعر
    ↓
المبيعات ينشئ طلب (isQuotation: true)
    ↓
قسم الحسابات ينشئ عرض السعر
    ↓
مراجعة وموافقة داخلية
    ↓
إرسال عرض السعر للعميل
    ↓
العميل يوافق
    ↓
تحويل عرض السعر لطلب تنفيذ
    ↓
بعد اكتمال التنفيذ
    ↓
قسم الحسابات يصدر الفاتورة
    ↓
إرسال الفاتورة للعميل
    ↓
تسجيل المدفوعات
    ↓
إغلاق الفاتورة (مدفوعة بالكامل)
```

### 2. من طلب مباشر إلى فاتورة

```
العميل يطلب تنفيذ مباشر
    ↓
المبيعات ينشئ طلب (isQuotation: false)
    ↓
مراجعة المدير التنفيذي
    ↓
بدء التنفيذ (تصميم → طباعة)
    ↓
بعد اكتمال الطباعة
    ↓
قسم الحسابات يصدر الفاتورة
    ↓
إرسال الفاتورة للعميل
    ↓
تسجيل المدفوعات
    ↓
بعد الدفع الكامل
    ↓
الإرسال للعميل
```

---

## 💻 الواجهة والاستخدام

### لوحة التحكم

**الرابط:** `http://localhost:3000/accounting`

#### الإحصائيات المعروضة:
1. **المدفوعات المستلمة**
   - إجمالي المدفوعات
   - المدفوعات هذا الشهر

2. **المبالغ المعلقة**
   - إجمالي المبالغ المعلقة
   - عدد الفواتير المعلقة

3. **عروض الأسعار**
   - العدد الإجمالي
   - قيد الانتظار

4. **الفواتير**
   - العدد الإجمالي
   - المدفوعة
   - المتأخرة

#### الإجراءات السريعة:
- ➕ إنشاء عرض سعر جديد
- ➕ إنشاء فاتورة جديدة
- 📋 إدارة عروض الأسعار
- 🧾 إدارة الفواتير

---

## 📊 التقارير

### 1. تقرير عروض الأسعار

**يتضمن:**
- إجمالي عروض الأسعار
- عروض الأسعار المقبولة vs المرفوضة
- معدل التحويل (Conversion Rate)
- متوسط قيمة عرض السعر
- الوقت المتوسط للرد

### 2. تقرير الفواتير

**يتضمن:**
- إجمالي الفواتير الصادرة
- الفواتير المدفوعة بالكامل
- الفواتير الجزئية
- الفواتير المتأخرة
- المبالغ المستحقة

### 3. التقرير المالي

**يتضمن:**
- إجمالي المبيعات
- إجمالي الإيرادات المحصلة
- المستحقات المعلقة
- المستحقات المتأخرة
- التوزيع الشهري

---

## 🎯 المهام اليومية

### للمحاسب:

#### الصباح (9:00 - 12:00):
1. ✅ مراجعة الفواتير المتأخرة
2. ✅ متابعة العملاء للدفع
3. ✅ إنشاء عروض أسعار جديدة
4. ✅ مراجعة الطلبات الجديدة

#### المساء (1:00 - 5:00):
1. ✅ إصدار الفواتير للطلبات المكتملة
2. ✅ تسجيل المدفوعات المستلمة
3. ✅ متابعة عروض الأسعار المرسلة
4. ✅ تحديث حالات الفواتير

### لمدير الحسابات:

#### يومياً:
1. ✅ مراجعة الإحصائيات المالية
2. ✅ الموافقة على عروض الأسعار
3. ✅ الموافقة على الفواتير
4. ✅ متابعة المتأخرات

#### أسبوعياً:
1. ✅ إعداد التقارير الأسبوعية
2. ✅ مراجعة الأداء المالي
3. ✅ اجتماع مع الإدارة

#### شهرياً:
1. ✅ التقرير المالي الشهري
2. ✅ تحليل معدلات التحويل
3. ✅ تقييم أداء القسم

---

## 🔐 الأمان والصلاحيات

### قواعد الوصول:

1. **عروض الأسعار:**
   - المحاسب: يمكنه إنشاء وتعديل عروض الأسعار التي أنشأها
   - مدير الحسابات: يمكنه عرض وتعديل جميع عروض الأسعار
   - CEO: يمكنه عرض جميع عروض الأسعار

2. **الفواتير:**
   - المحاسب: يمكنه إنشاء وتعديل الفواتير التي أنشأها
   - مدير الحسابات: يمكنه عرض وتعديل جميع الفواتير
   - CEO: يمكنه عرض جميع الفواتير

3. **المدفوعات:**
   - المحاسب ومدير الحسابات: يمكنهم تسجيل المدفوعات
   - جميع سجلات المدفوعات يجب أن تحتوي على:
     - المبلغ
     - طريقة الدفع
     - التاريخ
     - رقم مرجعي (إن وجد)
     - اسم المستلم

---

## 📝 نماذج الاستخدام

### مثال 1: إنشاء عرض سعر

```typescript
// 1. العميل يطلب عرض سعر
const order = {
  customerName: "شركة الأمل",
  customerPhone: "+966501234567",
  isQuotation: true,
  // ... باقي التفاصيل
};

// 2. المحاسب ينشئ عرض السعر
const quotation = {
  quotationNumber: "QT-2025-0025",
  customerName: "شركة الأمل",
  items: [
    { description: "طباعة كروت شخصية", quantity: 1000, unitPrice: 0.5 }
  ],
  subtotal: 500,
  taxRate: 15,
  taxAmount: 75,
  totalAmount: 575,
  validUntil: "2025-12-15",
  paymentTerms: {
    method: "نقدي أو آجل 30 يوم",
    downPaymentPercentage: 50
  }
};

// 3. إرسال للموافقة الداخلية
// 4. الموافقة وإرسال للعميل
// 5. العميل يوافق → تحويل لطلب
```

### مثال 2: إصدار فاتورة

```typescript
// 1. بعد اكتمال الطلب
const invoice = {
  invoiceNumber: "INV-2025-0050",
  invoiceType: "sales",
  relatedOrderId: "order-123",
  customerName: "شركة الأمل",
  items: [
    { description: "كروت شخصية - 1000 قطعة", quantity: 1, unitPrice: 575 }
  ],
  subtotal: 500,
  taxAmount: 75,
  totalAmount: 575,
  paidAmount: 0,
  remainingAmount: 575,
  dueDate: "2025-12-30"
};

// 2. إرسال للعميل
// 3. تسجيل الدفعات
invoice.paymentRecords.push({
  amount: 575,
  paymentMethod: "تحويل بنكي",
  paymentDate: "2025-12-28",
  reference: "TRX-123456",
  receivedBy: user.uid,
  receivedByName: user.displayName
});

// 4. تحديث الحالة
invoice.paidAmount = 575;
invoice.remainingAmount = 0;
invoice.status = "invoice_fully_paid";
```

---

## 🚀 البدء السريع

### 1. تسجيل الدخول
```
الرابط: http://localhost:3000/login
البريد: accountant@najd.com
كلمة المرور: Najd@2024
```

### 2. الوصول للوحة التحكم
```
http://localhost:3000/accounting
```

### 3. إنشاء أول عرض سعر
1. اضغط على "إنشاء عرض سعر جديد"
2. أدخل معلومات العميل
3. أضف بنود عرض السعر
4. حدد شروط الدفع والصلاحية
5. احفظ واطلب الموافقة

### 4. إصدار أول فاتورة
1. اختر طلب مكتمل
2. اضغط على "إصدار فاتورة"
3. راجع التفاصيل والمبلغ
4. أصدر الفاتورة
5. أرسلها للعميل

---

## 📚 الملفات ذات الصلة

| الملف | الوصف |
|------|-------|
| `packages/shared/src/types/quotation.types.ts` | أنواع عروض الأسعار |
| `packages/shared/src/types/invoice.types.ts` | أنواع الفواتير |
| `packages/shared/src/types/user.types.ts` | صلاحيات قسم الحسابات |
| `packages/shared/src/types/order.types.ts` | ربط الطلبات بعروض الأسعار والفواتير |
| `apps/web/src/app/accounting/page.tsx` | لوحة تحكم قسم الحسابات |

---

## ✅ قائمة التحقق

### عند إنشاء عرض سعر:
- [ ] معلومات العميل كاملة
- [ ] الرقم الضريبي (للشركات)
- [ ] بنود العرض واضحة ومفصلة
- [ ] الأسعار صحيحة وشاملة الضريبة
- [ ] شروط الدفع محددة
- [ ] تاريخ الصلاحية مناسب
- [ ] المراجعة اللغوية

### عند إصدار فاتورة:
- [ ] الطلب مكتمل ومُسلّم
- [ ] معلومات العميل صحيحة
- [ ] المبلغ مطابق للاتفاق
- [ ] الضريبة محسوبة بشكل صحيح (15%)
- [ ] تاريخ الاستحقاق محدد
- [ ] رقم الفاتورة تسلسلي

### عند تسجيل دفعة:
- [ ] المبلغ صحيح
- [ ] طريقة الدفع محددة
- [ ] الرقم المرجعي مسجل (شيك/تحويل)
- [ ] إصدار سند قبض
- [ ] تحديث حالة الفاتورة

---

## 🆘 الدعم والمساعدة

### الأسئلة الشائعة:

**س: كيف أنشئ عرض سعر؟**
ج: من لوحة التحكم → إنشاء عرض سعر جديد → املأ التفاصيل → احفظ

**س: ماذا أفعل إذا تأخر العميل في الدفع؟**
ج: النظام يحدث الحالة تلقائياً لـ "متأخرة" - تابع مع العميل وسجل كل اتصال

**س: كيف أسجل دفعة جزئية؟**
ج: من صفحة الفاتورة → تسجيل دفعة → أدخل المبلغ المستلم

**س: هل يمكن تعديل فاتورة بعد إصدارها؟**
ج: يمكن التعديل قبل الإرسال للعميل، بعد الإرسال يجب إصدار إشعار دائن/مدين

---

## 📞 جهات الاتصال

**قسم الحسابات:**
- الهاتف الداخلي: 105
- البريد الإلكتروني: accounting@najd.com

**مدير الحسابات:**
- الهاتف الداخلي: 101
- البريد الإلكتروني: accounting.head@najd.com

---

© 2024 شركة نجد - جميع الحقوق محفوظة


