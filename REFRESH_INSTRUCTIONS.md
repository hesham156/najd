# تعليمات تحديث التطبيق 🔄

## ✅ تم إعادة تشغيل الخادم

الخادم يعمل الآن على: **http://localhost:3000**

## 🔄 خطوات تحديث الصفحة

### الطريقة 1: إعادة تحميل قوية (موصى بها) 💪

في المتصفح، اضغط:

- **Windows/Linux:** `Ctrl + Shift + R` أو `Ctrl + F5`
- **Mac:** `Cmd + Shift + R`

هذا سيقوم بـ:
- ✅ تفريغ الذاكرة المؤقتة (Cache)
- ✅ تحميل أحدث نسخة من الكود
- ✅ تطبيق جميع التعديلات

### الطريقة 2: مسح الذاكرة المؤقتة يدوياً

1. افتح Developer Tools: `F12`
2. انقر بزر الماوس الأيمن على زر التحديث 🔄
3. اختر: **"Empty Cache and Hard Reload"**

### الطريقة 3: إعادة فتح الصفحة

1. أغلق نافذة المتصفح تماماً
2. افتح: http://localhost:3000

## 🧪 التحقق من التحديث

بعد إعادة التحميل:

1. **افتح Developer Console:** `F12` → Console
2. **تأكد من عدم وجود أخطاء CORS**
3. **جرب إنشاء طلب جديد**

يجب أن **لا** ترى هذا الخطأ:
```
❌ POST https://us-central1-najd-5e7c7.cloudfunctions.net/generateOrderNumber
```

بدلاً من ذلك، يجب أن يعمل بدون أخطاء! ✅

## 📋 الخطوات الكاملة للاختبار

### 1. تسجيل الدخول
```
http://localhost:3000/login

البريد الإلكتروني: ceo@najd.com
كلمة المرور: Najd@2024
```

### 2. إنشاء طلب جديد
```
http://localhost:3000/orders/new
```

### 3. ملء البيانات:
- اسم العميل
- رقم الهاتف
- نوع الطباعة
- الكمية
- الأولوية

### 4. انقر "إرسال الطلب"

### 5. النتيجة المتوقعة ✅
- رسالة نجاح: "تم إرسال الطلب للمراجعة بنجاح"
- إعادة توجيه لصفحة الطلبات
- رقم طلب بالصيغة: `NAJD-2025-0001`

## 🐛 إذا استمرت المشكلة

### 1. تحقق من الخادم

افتح نافذة PowerShell التي تشغل الخادم، يجب أن ترى:
```
✓ Ready in Xms
○ Local: http://localhost:3000
```

### 2. تحقق من الكود في Developer Tools

افتح: `F12` → Sources → `page.tsx`

ابحث عن هذا السطر:
```javascript
const orderNumber = await generateOrderNumber();
```

**يجب أن لا ترى:**
```javascript
const generateOrderNumber = httpsCallable(functions, 'generateOrderNumber');
```

### 3. إعادة تشغيل الخادم يدوياً

إذا لم يعمل، أعد تشغيل الخادم:

```powershell
# أغلق النافذة القديمة أو اضغط Ctrl+C
cd D:\najd\apps\web
npm run dev
```

## 💡 نصائح

### تجنب هذه المشكلة مستقبلاً:

1. **استخدم Hard Reload دائماً** عند إجراء تغييرات
2. **راقب نافذة الخادم** للتأكد من إعادة البناء
3. **انتظر رسالة "compiled successfully"** قبل الاختبار

### Next.js Hot Reload

Next.js يدعم Hot Module Replacement (HMR):
- التغييرات في الملفات تُطبق تلقائياً
- **لكن في بعض الأحيان** تحتاج لإعادة تحميل يدوية
- خاصة عند تغيير:
  - Imports
  - Firebase configuration
  - Environment variables

## 📊 الملخص السريع

| الخطوة | الأمر/الإجراء |
|--------|---------------|
| 1️⃣ تحديث الصفحة | `Ctrl + Shift + R` |
| 2️⃣ افتح DevTools | `F12` |
| 3️⃣ تحقق من Console | لا أخطاء CORS |
| 4️⃣ سجل الدخول | ceo@najd.com |
| 5️⃣ جرب إنشاء طلب | يجب أن يعمل ✅ |

## ✅ ما تم إصلاحه

- ❌ **القديم:** استدعاء Cloud Function → CORS error
- ✅ **الجديد:** توليد محلي → يعمل بسلاسة

## 🎯 النتيجة المتوقعة

عند إنشاء طلب جديد:

```javascript
// في Console (F12)
✓ Order number generated: NAJD-2025-0001
✓ Files uploaded successfully
✓ Order created in Firestore
✓ Redirecting to orders page...
```

**لا يجب أن ترى أي أخطاء CORS!** 🎉

---

## 🆘 المساعدة

إذا استمرت المشكلة:

1. تأكد من تحديث الصفحة بـ `Ctrl + Shift + R`
2. تحقق من أن الخادم يعمل في نافذة PowerShell
3. راجع Developer Console للأخطاء
4. اقرأ `CORS_FIX.md` لفهم التغييرات

---

© 2024 شركة نجد - جميع الحقوق محفوظة


