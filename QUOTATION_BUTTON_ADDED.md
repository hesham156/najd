# ✅ إضافة زر "إنشاء عرض سعر"

## 🎉 التحديث

تم إضافة زر **"إنشاء عرض سعر"** في صفحة تفاصيل الطلب لقسم الحسابات!

---

## 📍 الموقع

### صفحة تفاصيل الطلب:
```
http://localhost:3000/orders/[id]
```

عندما يفتح موظف الحسابات طلب عرض سعر (`isQuotation: true`):

```
┌─────────────────────────────────────┐
│ تفاصيل الطلب NAJD-2025-0002         │
│ [💰 عرض سعر]                       │
├─────────────────────────────────────┤
│                                     │
│  ┌────────────────────────────┐    │
│  │ إجراءات عروض الأسعار       │    │
│  ├────────────────────────────┤    │
│  │                            │    │
│  │  [➕ إنشاء عرض سعر]        │    │ ← الزر الجديد!
│  │                            │    │
│  │  قم بإنشاء عرض سعر لهذا    │    │
│  │  الطلب                     │    │
│  └────────────────────────────┘    │
└─────────────────────────────────────┘
```

---

## 🔧 التحديثات

### 1. ✅ زر في صفحة تفاصيل الطلب
**الملف:** `apps/web/src/app/orders/[id]/page.tsx`

**الشروط:**
- يظهر فقط لـ `user.department === 'accounting'`
- يظهر فقط للطلبات التي `isQuotation: true`

**الحالتان:**

#### أ) إذا لم يتم إنشاء عرض سعر بعد:
```typescript
{!(order as any).quotationId && (
  <button onClick={() => router.push(`/accounting/quotations/new?orderId=${order.id}`)}>
    ➕ إنشاء عرض سعر
  </button>
)}
```

#### ب) إذا تم إنشاء عرض سعر:
```typescript
{(order as any).quotationId && (
  <button onClick={() => router.push(`/accounting/quotations/${order.quotationId}`)}>
    📄 عرض عرض السعر
  </button>
  <p>✓ تم إنشاء عرض السعر: {order.quotationNumber}</p>
)}
```

---

### 2. ✅ صفحة إنشاء عرض السعر (Placeholder)
**الملف الجديد:** `apps/web/src/app/accounting/quotations/new/page.tsx`

**الموقع:** `http://localhost:3000/accounting/quotations/new?orderId=xxx`

**المحتوى الحالي:**
- عرض معلومات الطلب المرتبط
- رسالة "قريباً" للنموذج
- قائمة بالميزات القادمة
- أزرار للعودة

---

## 🎯 كيفية الاستخدام

### الخطوة 1: المبيعات ينشئ طلب عرض سعر
```
1. http://localhost:3000/orders/new
2. املأ معلومات العميل والمنتجات
3. ✅ فعّل "طلب عرض سعر"
4. اضغط "إنشاء الطلب"
```

### الخطوة 2: الحسابات يفتح الطلب
```
1. http://localhost:3000/orders
2. اضغط على الطلب لفتح تفاصيله
3. في الـ Sidebar الأيسر، سترى:
   ┌───────────────────────┐
   │ إجراءات عروض الأسعار │
   │ [➕ إنشاء عرض سعر]    │
   └───────────────────────┘
```

### الخطوة 3: إنشاء عرض السعر
```
1. اضغط على "إنشاء عرض سعر"
2. سيتم نقلك للصفحة الجديدة
3. (حالياً: صفحة placeholder - قيد التطوير)
```

---

## 📊 السير الكامل

```
المبيعات                    الحسابات
    ↓                           ↓
إنشاء طلب               يفتح صفحة الطلب
    ↓                           ↓
✅ طلب عرض سعر          يرى زر "إنشاء عرض سعر"
    ↓                           ↓
حفظ الطلب              يضغط على الزر
    ↓                           ↓
الطلب يظهر             ينتقل لصفحة الإنشاء
للحسابات                       ↓
                        (قيد التطوير)
```

---

## 🎨 تصميم الزر

### الألوان:
- **الزر الأزرق:** `bg-gradient-to-r from-blue-500 to-blue-600`
- **الزر الأخضر (بعد الإنشاء):** `bg-gradient-to-r from-green-500 to-green-600`

### الأيقونات:
- **إنشاء:** `➕` (علامة زائد)
- **عرض:** `📄` (مستند)
- **تأكيد:** `✓` (علامة صح)

---

## 📝 الملفات المعدلة

| الملف | التعديل | الحالة |
|------|---------|--------|
| `apps/web/src/app/orders/[id]/page.tsx` | إضافة زر في Sidebar | ✅ مكتمل |
| `apps/web/src/app/accounting/quotations/new/page.tsx` | صفحة placeholder | ✅ مكتمل |

---

## 🚀 الخطوات القادمة

### المرحلة التالية - تطوير نموذج الإنشاء:

1. **نموذج إضافة البنود**
   - إضافة/حذف/تعديل بنود عرض السعر
   - وصف، كمية، سعر الوحدة

2. **الحسابات التلقائية**
   - حساب المجموع الفرعي
   - حساب الضريبة (15%)
   - حساب الخصم (إن وجد)
   - المجموع الإجمالي

3. **شروط الدفع والتسليم**
   - طريقة الدفع
   - نسبة الدفعة المقدمة
   - شروط الدفع المتبقي
   - تاريخ التسليم

4. **الصلاحية**
   - تاريخ إصدار عرض السعر
   - تاريخ انتهاء الصلاحية (افتراضي: 14 يوم)

5. **الحفظ والربط**
   - حفظ عرض السعر في collection "quotations"
   - تحديث الطلب بـ `quotationId` و `quotationNumber`
   - إرسال إشعار لمدير الحسابات

6. **سير الموافقة**
   - طلب الموافقة من مدير الحسابات
   - إرسال للعميل بعد الموافقة
   - تتبع حالة عرض السعر

---

## ✅ ما تم إنجازه حتى الآن

| المهمة | الحالة |
|--------|--------|
| ✅ تحديث صلاحيات قسم الحسابات | مكتمل |
| ✅ إنشاء أنواع عروض الأسعار | مكتمل |
| ✅ تحديث Firestore Rules | مكتمل ومنشور |
| ✅ تحديث useOrders Hook | مكتمل |
| ✅ إضافة Firestore Index | مكتمل ومنشور |
| ✅ زر "إنشاء عرض سعر" | مكتمل |
| ✅ صفحة placeholder | مكتمل |
| ⏳ نموذج الإنشاء الكامل | قيد التطوير |

---

## 🎓 للاختبار

### 1. كمحاسب:
```bash
# سجل دخول كمحاسب
http://localhost:3000/login

# افتح صفحة الطلبات
http://localhost:3000/orders

# افتح أي طلب يحمل علامة "💰 عرض سعر"
# ستجد في الـ Sidebar:
# [➕ إنشاء عرض سعر]
```

### 2. اضغط على الزر:
```
سيتم نقلك إلى:
http://localhost:3000/accounting/quotations/new?orderId=xxx
```

### 3. النتيجة:
```
صفحة "قريباً" مع:
- ✅ معلومات الطلب
- ✅ قائمة الميزات القادمة
- ✅ أزرار العودة
```

---

## 📚 ملفات ذات صلة

| الملف | الوصف |
|------|-------|
| `ACCOUNTING_DEPARTMENT.md` | دليل قسم الحسابات الشامل |
| `QUOTATION_WORKFLOW.md` | سير العمل التفصيلي |
| `SALES_TO_ACCOUNTING_FLOW.md` | السير المبسط |
| `ACCOUNTING_UPDATES.md` | ملخص جميع التحديثات |
| `QUOTATION_BUTTON_ADDED.md` | هذا الملف |

---

## ✨ الخلاصة

✅ **الطلبات تظهر** لقسم الحسابات

✅ **الزر موجود** في صفحة تفاصيل الطلب

✅ **الصفحة جاهزة** (placeholder مؤقت)

⏳ **النموذج الكامل** قيد التطوير

---

**كل شيء يعمل بشكل صحيح! الخطوة التالية هي تطوير نموذج الإنشاء الكامل.** 🎉

---

© 2024 شركة نجد - جميع الحقوق محفوظة


