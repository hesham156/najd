# ❓ لماذا يظهر عرض سعر واحد فقط؟

## 🔍 التوضيح

يظهر عرض سعر واحد فقط لأن **عرض السعر ≠ الطلب**!

---

## 📋 الفرق بين الطلب وعرض السعر

### الطلب (Order)
```
- ينشئه المبيعات ✅
- يحفظ في collection "orders"
- يمكن أن يكون:
  • طلب طباعة عادي (isQuotation: false)
  • طلب عرض سعر (isQuotation: true)
```

### عرض السعر (Quotation)
```
- ينشئه الحسابات ✅
- يحفظ في collection "quotations"
- يُنشأ فقط عندما:
  1. يكون هناك طلب بـ isQuotation: true
  2. الحسابات يفتح الطلب
  3. الحسابات يضغط "إنشاء عرض سعر"
  4. الحسابات يملأ النموذج ويحفظ
```

---

## 🔄 السير الصحيح

```
1. المبيعات → ينشئ "طلب عرض سعر"
   النتيجة: Order في "orders" (isQuotation: true)
   ✅ الطلب موجود
   ❌ عرض السعر غير موجود بعد

2. الحسابات → يفتح الطلب
   يرى زر "إنشاء عرض سعر"
   ↓
   يضغط على الزر
   ↓
   يملأ نموذج عرض السعر (البنود، الأسعار، الشروط)
   ↓
   يحفظ
   النتيجة: Quotation في "quotations"
   ✅ الطلب موجود
   ✅ عرض السعر موجود الآن

3. CEO → يوافق على عرض السعر
   ↓
   المبيعات يراه في /quotations
```

---

## 💡 لماذا يظهر واحد فقط؟

### السبب:

لديك **عدة طلبات** لكن **عرض سعر واحد فقط**!

```
الطلبات (Orders):
✅ NAJD-2025-0001 (isQuotation: true) → لم يُنشأ عرض سعر بعد
✅ NAJD-2025-0002 (isQuotation: true) → لم يُنشأ عرض سعر بعد
✅ NAJD-2025-0008 (isQuotation: true) → ✅ تم إنشاء عرض سعر QT-2025-0003

عروض الأسعار (Quotations):
✅ QT-2025-0003 (للطلب NAJD-2025-0008)
```

**صفحة /quotations تعرض "عروض الأسعار" وليس "الطلبات"!**

---

## ✅ الحل

### لعرض جميع عروض الأسعار:

يجب أن **الحسابات ينشئ عرض سعر** لكل طلب!

### الخطوات:

1. **كـ حسابات:**
   ```
   افتح /orders
   → افتح طلب NAJD-2025-0001 (يحمل 💰 عرض سعر)
   → اضغط "إنشاء عرض سعر"
   → املأ النموذج
   → احفظ
   ```

2. **كرر للطلب الآخر:**
   ```
   افتح طلب NAJD-2025-0002
   → اضغط "إنشاء عرض سعر"
   → املأ النموذج
   → احفظ
   ```

3. **الآن افتح /quotations:**
   ```
   ستجد 3 عروض أسعار! ✅
   ```

---

## 📊 للتحقق

### 1. افتح صفحة الطلبات (كـ حسابات):
```
http://localhost:3000/orders
```

**ابحث عن الطلبات التي تحمل "💰 عرض سعر"**

### 2. لكل طلب:
```
- افتح الطلب
- في الـ Sidebar:
  
  إذا ظهر [إنشاء عرض سعر] ← لم يُنشأ بعد
  إذا ظهر [عرض عرض السعر] ← تم إنشاؤه ✅
```

### 3. أنشئ عروض الأسعار الناقصة:
```
اضغط "إنشاء عرض سعر"
→ املأ النموذج
→ احفظ
```

---

## 🎯 الخلاصة

**ما يظهر في /quotations:**
- ✅ عروض الأسعار التي **تم إنشاؤها** من قسم الحسابات
- ❌ ليس الطلبات

**لرؤية المزيد من عروض الأسعار:**
- يجب أن الحسابات ينشئها أولاً!

---

**هذا التصميم صحيح ومقصود:**
- الطلب = طلب من العميل
- عرض السعر = مستند تسعير يُعده الحسابات

**ليس كل طلب يحتاج عرض سعر!** ✅

---

© 2024 شركة نجد


