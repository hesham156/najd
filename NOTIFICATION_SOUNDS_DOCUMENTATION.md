# 🔔 نظام التنبيهات الصوتية - Najd Company

<div dir="rtl">

**تاريخ الإنشاء**: 6 نوفمبر 2025  
**الحالة**: ✅ مكتمل

---

## 📋 نظرة عامة

تم إضافة **نظام تنبيهات صوتية ذكي** يعمل تلقائياً عند:
- 💬 استقبال رسالة نصية جديدة
- 🎙️ استقبال رسالة صوتية
- 📞 ورود مكالمة صوتية
- ✅ إرسال رسالة بنجاح

---

## 🎵 أنواع الأصوات

### 1. صوت رسالة نصية جديدة 💬

**النغمة**: C → E → G (ثلاث نغمات سريعة)
```
♪ دو - مي - صول ♪
```

**المدة**: 0.5 ثانية  
**الحجم**: هادئ (30%)

**متى يُشغّل**:
- عند استقبال رسالة نصية من الطرف الآخر
- فقط إذا كنت في المحادثة أو في قائمة المحادثات

### 2. صوت رسالة صوتية 🎙️

**النغمة**: A → C# (نغمتين أعمق)
```
♪ لا - دو# ♪
```

**المدة**: 0.6 ثانية  
**الحجم**: هادئ (25%)  
**النوع**: Triangle wave (نغمة ناعمة)

**متى يُشغّل**:
- عند استقبال رسالة صوتية (تسجيل)
- يختلف عن الرسالة النصية ليميز بينهما

### 3. صوت مكالمة واردة 📞

**النغمة**: رنين متكرر (800 Hz)
```
♪ رنّ رنّ ... رنّ رنّ ♪
```

**المدة**: 0.4 ثانية × 2 (مع فاصل)  
**الحجم**: متوسط (40%)

**متى يُشغّل**:
- عند ورود مكالمة صوتية
- يتكرر حتى الرد أو الرفض

### 4. صوت إرسال رسالة ✅

**النغمة**: نغمة قصيرة (800 Hz)
```
♪ تِك ♪
```

**المدة**: 0.15 ثانية  
**الحجم**: هادئ جداً (15%)

**متى يُشغّل**:
- عند إرسال رسالة نصية بنجاح
- عند إرسال تسجيل صوتي بنجاح

---

## 🔧 التقنيات المستخدمة

### Web Audio API:

```typescript
// إنشاء AudioContext
const audioContext = new AudioContext();

// إنشاء مولد نغمات
const oscillator = audioContext.createOscillator();

// التحكم بالصوت
const gainNode = audioContext.createGain();

// ربط العقد
oscillator.connect(gainNode);
gainNode.connect(audioContext.destination);

// تعيين النغمة والحجم
oscillator.frequency.value = 523.25; // نغمة C
gainNode.gain.value = 0.3; // حجم 30%

// تشغيل
oscillator.start();
oscillator.stop(audioContext.currentTime + 0.5);
```

---

## 🎛️ التحكم في الأصوات

### زر كتم/إلغاء كتم:

في **قائمة المحادثات** (أعلى اليسار):
- 🔊 **أيقونة السماعة الزرقاء**: الأصوات مفعّلة
- 🔇 **أيقونة السماعة الرمادية مشطوبة**: الأصوات مكتومة

**اضغط عليها** للتبديل بين الوضعين.

### حفظ الإعدادات:

```typescript
// يتم حفظ حالة الكتم في localStorage
localStorage.setItem('chatSoundsMuted', 'true/false');

// يتم استرجاعها تلقائياً عند فتح الصفحة
```

---

## 🎯 كيف يعمل النظام

### عند استقبال رسالة جديدة:

```typescript
// 1. مراقبة عدد الرسائل
const previousCount = 5;
const currentCount = 6;  // رسالة جديدة!

// 2. التحقق من المرسل
if (newMessage.senderId !== currentUserId) {
  // 3. تحديد نوع الصوت
  if (newMessage.type === 'audio') {
    playVoiceMessageSound();  // 🎙️ صوت رسالة صوتية
  } else {
    playMessageSound();        // 💬 صوت رسالة نصية
  }
}
```

### عند إرسال رسالة:

```typescript
// بعد نجاح الإرسال
await sendMessage(text);
playSentSound();  // ✅ صوت تأكيد الإرسال
```

---

## 🎨 التخصيص

### تغيير النغمات:

في `apps/web/src/hooks/useNotificationSound.ts`:

```typescript
// صوت رسالة جديدة
const playMessageSound = () => {
  oscillator.frequency.value = 523.25;  // غيّر النغمة هنا
  gainNode.gain.value = 0.3;            // غيّر الحجم هنا
  // ...
};
```

### النغمات الموسيقية المتاحة:

| نوتة | تردد (Hz) | الاستخدام |
|------|----------|-----------|
| C | 261.63 | نغمة منخفضة |
| D | 293.66 | نغمة معتدلة |
| E | 329.63 | نغمة لطيفة |
| F | 349.23 | نغمة دافئة |
| G | 392.00 | نغمة مريحة |
| A | 440.00 | النغمة القياسية |
| B | 493.88 | نغمة عالية |
| C (عالي) | 523.25 | نغمة واضحة |

---

## 🔊 الحجم والمستوى

### مستويات الصوت الموصى بها:

| النوع | الحجم | السبب |
|-------|-------|-------|
| رسالة جديدة | 0.3 (30%) | واضح لكن غير مزعج |
| رسالة صوتية | 0.25 (25%) | أهدأ قليلاً |
| مكالمة واردة | 0.4 (40%) | أعلى لجذب الانتباه |
| إرسال رسالة | 0.15 (15%) | تأكيد خفيف |

---

## 📱 التوافق

### المتصفحات:

- ✅ **Chrome/Edge** - يعمل ممتاز
- ✅ **Firefox** - يعمل ممتاز
- ✅ **Safari** - يعمل (قد يحتاج تفاعل مستخدم أولاً)
- ✅ **Opera** - يعمل ممتاز

### الأنظمة:

- ✅ **Windows** - يعمل
- ✅ **macOS** - يعمل
- ✅ **Linux** - يعمل
- ✅ **Mobile Web** - يعمل (مع قيود)

---

## ⚙️ الإعدادات المتقدمة

### تعطيل الأصوات افتراضياً:

في `useNotificationSound.ts`:

```typescript
const [isMuted, setIsMuted] = useState(true); // ابدأ مكتوماً
```

### إضافة أصوات مخصصة:

```typescript
// استخدام ملفات صوتية حقيقية
const audio = new Audio('/sounds/notification.mp3');
audio.volume = 0.3;
audio.play();
```

---

## 🎯 السلوك الذكي

### لا يُشغّل صوت:

- ❌ عند تحميل المحادثة لأول مرة
- ❌ للرسائل القديمة
- ❌ لرسائلك أنت (فقط صوت إرسال خفيف)
- ❌ عند كتم الأصوات

### يُشغّل صوت:

- ✅ للرسائل الجديدة فقط
- ✅ من المستخدمين الآخرين
- ✅ في الوقت الفعلي
- ✅ إذا كانت الأصوات مفعّلة

---

## 📊 الأداء

### تحسينات:

1. **إعادة استخدام AudioContext**:
   ```typescript
   // لا ننشئ context جديد في كل مرة
   if (!audioContextRef.current) {
     audioContextRef.current = new AudioContext();
   }
   ```

2. **إيقاف تلقائي**:
   ```typescript
   // الأصوات تتوقف تلقائياً بعد انتهائها
   oscillator.stop(audioContext.currentTime + duration);
   ```

3. **عدم حظر UI**:
   - الأصوات تُشغّل بشكل async
   - لا تؤثر على الأداء

---

## 🎉 الآن جرب!

### 1. افتح محادثة

### 2. لاحظ زر 🔊 في الأعلى:
- **أزرق** = الأصوات مفعّلة ✅
- **رمادي** = الأصوات مكتومة 🔇

### 3. اضغط عليه للتبديل

### 4. جرب إرسال رسالة:
- ستسمع "تِك" خفيف عند الإرسال ✅

### 5. افتح نافذة أخرى (نفس الحساب أو حساب آخر):
- أرسل رسالة
- ستسمع "دو - مي - صول" عند الاستقبال! 🔔

### 6. جرب التسجيل الصوتي (Demo Mode):
- سترسل وتستقبل
- أصوات مختلفة لكل نوع! 🎵

---

## 🔧 استكشاف الأخطاء

### لا يُشغّل صوت؟

**1. تحقق من زر الكتم**:
- يجب أن يكون 🔊 (أزرق)

**2. تحقق من صوت النظام**:
- تأكد من عدم كتم صوت الكمبيوتر

**3. تحقق من صوت المتصفح**:
- بعض المتصفحات تمنع الصوت التلقائي
- تفاعل مع الصفحة أولاً (اضغط أي زر)

**4. افتح Console**:
- ابحث عن أخطاء AudioContext

---

## 🎊 الخلاصة

تم إضافة **نظام تنبيهات صوتية ذكي** يشمل:

✅ 4 أنواع أصوات مختلفة  
✅ كتم/إلغاء كتم سهل  
✅ حفظ الإعدادات تلقائياً  
✅ أصوات لطيفة وغير مزعجة  
✅ أداء ممتاز  
✅ توافق كامل  

**النظام جاهز ويعمل!** 🔔✨

---

## 📚 الملفات ذات الصلة:

- `apps/web/src/hooks/useNotificationSound.ts` - Hook الأصوات
- `apps/web/src/hooks/useChat.ts` - دمج الأصوات
- `apps/web/src/app/chat/page.tsx` - واجهة الكتم

**استمتع بالتنبيهات الصوتية!** 🎵🔔

</div>


