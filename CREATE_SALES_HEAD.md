# إنشاء مدير مبيعات 👨‍💼

<div dir="rtl">

## ⚠️ المشكلة الحالية:

أنت **موظف مبيعات** (sales)، لكن **لا يوجد مدير مبيعات** (sales_head) في النظام!

حسب قواعد الشات الهرمية:
- موظف المبيعات → يتواصل مع **مدير المبيعات** فقط
- إذا لم يوجد مدير → **لا يمكن إنشاء محادثات**

---

## ✅ الحل السريع - إنشاء مدير مبيعات:

### الطريقة 1: عبر Firebase Console (الأسهل) ⚡

#### الخطوة 1: إنشاء المستخدم في Authentication

1. افتح Firebase Console:
   ```
   https://console.firebase.google.com/project/najd-5e7c7
   ```

2. اذهب إلى: **Authentication** → **Users** → **Add user**

3. أدخل البيانات:
   ```
   Email: sales.head@najd.com
   Password: Najd@2024
   ```

4. اضغط **Add user**

5. **انسخ الـ UID** الذي يظهر (مثل: abc123xyz...)

#### الخطوة 2: إضافة البيانات في Firestore

1. اذهب إلى: **Firestore Database** → **users collection**

2. اضغط **Add document**

3. **Document ID**: الصق الـ UID الذي نسخته

4. أضف الحقول التالية:

| Field | Type | Value |
|-------|------|-------|
| `uid` | string | [نفس الـ UID] |
| `email` | string | `sales.head@najd.com` |
| `displayName` | string | `مدير المبيعات` |
| `role` | string | `sales_head` |
| `department` | string | `sales` |
| `isHead` | boolean | `true` |
| `isActive` | boolean | `true` |
| `createdAt` | timestamp | [now] |
| `updatedAt` | timestamp | [now] |

5. اضغط **Save**

---

### الطريقة 2: عبر السكريبت الموجود 📝

إذا كان لديك `serviceAccountKey.json`:

```bash
node scripts/create-user.js
```

ثم اختر:
- الدور: **3** (مدير المبيعات)
- البريد: `sales.head@najd.com`
- كلمة المرور: `Najd@2024`
- الاسم: `مدير المبيعات`

---

### الطريقة 3: عبر صفحة HTML 🌐

1. افتح الملف:
   ```
   scripts/create-user.html
   ```

2. في المتصفح، افتح الملف

3. أدخل البيانات:
   - البريد: `sales.head@najd.com`
   - كلمة المرور: `Najd@2024`
   - الاسم: `مدير المبيعات`
   - الدور: **مدير المبيعات**

4. اضغط **إنشاء مستخدم**

---

## 🎯 بعد إنشاء مدير المبيعات:

### 1. سجل خروج وأعد تحميل الصفحة

### 2. ارجع لصفحة الشات:
```
http://localhost:3000/chat
```

### 3. اضغط زر "+" (محادثة جديدة)

### 4. ستجد مدير المبيعات في القائمة! ✅

### 5. اختره وابدأ المحادثة 💬

---

## 📊 المستخدمون الموصى بإنشائهم:

للحصول على تجربة كاملة، أنشئ:

### المستخدمين الأساسيين:

1. ✅ **CEO** (موجود بالفعل؟)
   - `ceo@najd.com` / `Najd@2024`

2. ⚠️ **مدير المبيعات** (مطلوب لك!)
   - `sales.head@najd.com` / `Najd@2024`

3. **مدير التصميم**
   - `design.head@najd.com` / `Najd@2024`

4. **مدير الطباعة**
   - `printing.head@najd.com` / `Najd@2024`

5. **مدير الحسابات**
   - `accounting.head@najd.com` / `Najd@2024`

6. **مدير الإرسال**
   - `dispatch.head@najd.com` / `Najd@2024`

---

## 🔄 اختبار كامل:

### كموظف مبيعات:
1. سجل دخول: موظف مبيعات
2. افتح الشات
3. ستجد: **مدير المبيعات** فقط ✅

### كمدير مبيعات:
1. سجل دخول: مدير المبيعات
2. افتح الشات
3. ستجد: **CEO + جميع موظفي المبيعات** ✅

### كـ CEO:
1. سجل دخول: CEO
2. افتح الشات
3. ستجد: **جميع مدراء الأقسام** ✅

---

## 🎉 بعد إنشاء المدير:

النظام سيعمل بشكل كامل! 

**أنشئ المستخدم وجرب مرة أخرى!** 💪

</div>


