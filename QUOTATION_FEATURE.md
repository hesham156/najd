# ميزة طلب عرض السعر ✅

## 🎯 المطلوب

السماح لموظف المبيعات بتحديد إذا كان الطلب **طلب عرض سعر** (Quotation Request) أم طلب عادي.

## ✅ الميزة المضافة

### ما هو طلب عرض السعر؟

**طلب عرض السعر (Quotation)** هو:
- طلب يطلب فيه العميل تسعيرة أولاً
- لا يتم تنفيذه مباشرة
- يحتاج موافقة العميل على السعر قبل التنفيذ
- يتم وضع علامة خاصة عليه للتمييز

## 📝 التحديثات المطبقة

### 1. صفحة إنشاء الطلب

#### إضافة Checkbox:
```
┌──────────────────────────────────────┐
│ معلومات إضافية                      │
├──────────────────────────────────────┤
│ الأولوية: [متوسطة ▼]                │
│ تاريخ التسليم: [________]            │
├──────────────────────────────────────┤
│ ┌────────────────────────────────┐   │
│ │ ☑ 💰 طلب عرض سعر (Quotation)  │   │
│ │                                │   │
│ │ سيتم وضع علامة على هذا الطلب │   │
│ │ كطلب عرض سعر. لن يتم تنفيذه   │   │
│ │ مباشرة حتى الموافقة على السعر.│   │
│ └────────────────────────────────┘   │
└──────────────────────────────────────┘
```

**الموقع:** في قسم "معلومات إضافية"
**التصميم:** خلفية صفراء مميزة
**الوصف:** يظهر عند تفعيل الـ checkbox

### 2. الحفظ في قاعدة البيانات

```typescript
const orderData = {
  orderNumber: "NAJD-2025-0001",
  customerName: "أحمد محمد",
  // ... باقي الحقول
  isQuotation: true,  // ← الحقل الجديد
  tags: ["quotation"], // ← يضاف tag تلقائياً
  // ...
};
```

### 3. صفحة قائمة الطلبات

#### عرض العلامة:
```
┌────────────────┬──────────────┬─────────┐
│ رقم الطلب      │ العميل       │ الحالة  │
├────────────────┼──────────────┼─────────┤
│ NAJD-2025-0001 │ أحمد محمد    │ جديد    │
│ 💰 عرض سعر     │ 0501234567   │         │
├────────────────┼──────────────┼─────────┤
│ NAJD-2025-0002 │ فاطمة علي    │ قيد     │
│                │ 0559876543   │ التنفيذ │
└────────────────┴──────────────┴─────────┘
```

**العلامة:** شارة صفراء "💰 عرض سعر" بجانب رقم الطلب

### 4. صفحة تفاصيل الطلب

#### عرض العلامة:
```
┌────────────────────────────────────────┐
│ تفاصيل الطلب NAJD-2025-0001  [💰 عرض سعر] │
│                                        │
│ [جديد] [عالية]                        │
└────────────────────────────────────────┘
```

**العلامة:** شارة كبيرة صفراء بجانب عنوان الطلب

## 🎨 التصميم

### الألوان المستخدمة:

| العنصر | اللون | الكود |
|--------|-------|-------|
| **Checkbox في النموذج** | أصفر | `bg-yellow-50` `border-yellow-200` |
| **الشارة في القائمة** | أصفر فاتح | `bg-yellow-100` `text-yellow-800` |
| **الشارة في التفاصيل** | أصفر مميز | `bg-yellow-100` `border-yellow-400` |

### الأيقونة:
💰 - أيقونة كيس النقود للدلالة على عرض السعر

## 💾 بنية البيانات

### حقل جديد في Firestore:

```typescript
{
  orderNumber: "NAJD-2025-0001",
  customerName: "أحمد محمد",
  customerPhone: "0501234567",
  status: "pending_ceo_review",
  priority: "medium",
  
  // ← الحقل الجديد
  isQuotation: boolean,  // true = طلب عرض سعر
  
  // ← يتم إضافة tag تلقائياً
  tags: ["quotation"],   // إذا كان isQuotation = true
  
  // ... باقي الحقول
}
```

## 🔄 سير العمل (Workflow)

### طلب عادي:
```
إنشاء الطلب → مراجعة المدير → التصميم → الطباعة → التسليم
```

### طلب عرض سعر:
```
إنشاء طلب عرض سعر 
    ↓
إعداد التسعيرة
    ↓
إرسال السعر للعميل
    ↓
انتظار موافقة العميل
    ↓
إذا وافق → يتحول لطلب عادي ويكمل السير
إذا رفض → يتم إلغاء الطلب
```

## 🧪 كيفية الاستخدام

### للمبيعات:

1. **اذهب إلى صفحة إنشاء طلب جديد:**
   ```
   http://localhost:3000/orders/new
   ```

2. **املأ معلومات العميل والمنتجات**

3. **في قسم "معلومات إضافية":**
   - إذا كان العميل يريد **عرض سعر فقط**:
     ✅ فعّل checkbox "💰 طلب عرض سعر"
   
   - إذا كان طلب عادي (التنفيذ مباشر):
     ☐ اترك الـ checkbox بدون تفعيل

4. **أرسل الطلب**

### في قائمة الطلبات:

- طلبات عروض الأسعار تظهر بعلامة "💰 عرض سعر"
- سهل التمييز بينها وبين الطلبات العادية
- يمكن التصفية حسب النوع (مستقبلاً)

## 📊 مثال على الاستخدام

### مثال 1: عميل يريد عرض سعر

**السيناريو:**
- عميل اتصل: "أريد طباعة 1000 كرت شخصي، كم السعر؟"

**الإجراء:**
1. موظف المبيعات ينشئ طلب جديد
2. يملأ المعلومات:
   - العميل: أحمد محمد
   - المنتج: كرت شخصي
   - الكمية: 1000
3. ✅ يفعّل "طلب عرض سعر"
4. يرسل الطلب
5. يتم إعداد التسعيرة
6. يتم إرسال السعر للعميل
7. انتظار الرد

### مثال 2: طلب عادي

**السيناريو:**
- عميل دائم: "أريد نفس الطلب السابق"

**الإجراء:**
1. موظف المبيعات ينشئ طلب جديد
2. يملأ المعلومات
3. ☐ لا يفعّل "طلب عرض سعر"
4. يرسل الطلب للتنفيذ مباشرة

## 🎯 الفوائد

| الميزة | الفائدة |
|--------|---------|
| 🎯 **تمييز واضح** | سهولة معرفة نوع الطلب من النظرة الأولى |
| 📊 **تتبع أفضل** | معرفة عدد طلبات عروض الأسعار vs الطلبات العادية |
| ⏱️ **إدارة أفضل** | عدم البدء في التنفيذ قبل موافقة العميل |
| 💰 **تسعير دقيق** | وقت كافٍ لإعداد تسعيرة مفصلة |
| 📈 **إحصائيات** | معرفة معدل تحويل عروض الأسعار لطلبات فعلية |

## 📈 إحصائيات ممكنة (مستقبلاً)

يمكن إضافة:

### في لوحة التحكم:
```
┌─────────────────────────────────┐
│ إحصائيات طلبات عروض الأسعار     │
├─────────────────────────────────┤
│ إجمالي الطلبات: 50              │
│ عروض أسعار: 15 (30%)           │
│ طلبات عادية: 35 (70%)          │
├─────────────────────────────────┤
│ معدل التحويل:                   │
│ تم الموافقة عليها: 10 (67%)    │
│ تم الرفض: 3 (20%)              │
│ قيد الانتظار: 2 (13%)          │
└─────────────────────────────────┘
```

### تصفية في قائمة الطلبات:
```
☐ عروض أسعار فقط
☐ طلبات عادية فقط
```

## 🔮 تحسينات مستقبلية

### 1. حالات خاصة لعروض الأسعار
```typescript
enum QuotationStatus {
  PENDING = 'quotation_pending',      // قيد الإعداد
  SENT = 'quotation_sent',            // تم الإرسال
  APPROVED = 'quotation_approved',    // تمت الموافقة
  REJECTED = 'quotation_rejected',    // تم الرفض
  EXPIRED = 'quotation_expired',      // انتهت الصلاحية
}
```

### 2. تاريخ صلاحية عرض السعر
```typescript
{
  isQuotation: true,
  quotationExpiryDate: "2025-11-15",  // ← ينتهي بعد أسبوعين
  quotationPrice: 1500,               // ← السعر المقترح
  quotationNotes: "السعر شامل التصميم",
}
```

### 3. نموذج عرض سعر PDF
- توليد PDF تلقائي
- يحتوي على:
  - تفاصيل المنتجات
  - الأسعار
  - شروط الدفع
  - تاريخ الصلاحية

### 4. متابعة عروض الأسعار
- تنبيه بعد 3 أيام إذا لم يرد العميل
- تنبيه قبل انتهاء الصلاحية بيوم
- إحصائيات الرد (سريع/بطيء)

### 5. تحويل تلقائي
```typescript
// عند موافقة العميل
async function convertQuotationToOrder(orderId: string) {
  await updateDoc(doc(db, 'orders', orderId), {
    isQuotation: false,
    quotationApprovedAt: serverTimestamp(),
    status: OrderStatus.PENDING_CEO_REVIEW,
  });
}
```

## 📚 الملفات المحدثة

| الملف | التعديل |
|-------|---------|
| `apps/web/src/app/orders/new/page.tsx` | إضافة checkbox وحفظ isQuotation |
| `apps/web/src/app/orders/page.tsx` | عرض علامة عرض السعر في القائمة |
| `apps/web/src/app/orders/[id]/page.tsx` | عرض علامة عرض السعر في التفاصيل |

## ✅ الملخص

| العنصر | الحالة |
|--------|--------|
| Checkbox في النموذج | ✅ |
| حفظ في قاعدة البيانات | ✅ |
| عرض في قائمة الطلبات | ✅ |
| عرض في تفاصيل الطلب | ✅ |
| تصميم مميز | ✅ |
| سهولة الاستخدام | ✅ |

## 🎉 الميزة جاهزة!

**الآن يمكن للمبيعات:**
- ✅ تحديد نوع الطلب بسهولة
- ✅ تمييز طلبات عروض الأسعار
- ✅ إدارة أفضل للطلبات
- ✅ متابعة دقيقة

**جرّب الميزة الآن!** 🚀

---

© 2024 شركة نجد - جميع الحقوق محفوظة


