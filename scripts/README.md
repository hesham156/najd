# أدوات إنشاء المستخدمين 👥

## 📋 نظرة عامة

هذا المجلد يحتوي على أدوات لإنشاء مستخدمين جدد في نظام نجد.

## 🚀 الطريقة الأولى: صفحة الويب (الأسهل والموصى بها)

### الخطوات:

1. **افتح الصفحة في المتصفح:**
   ```bash
   # من مجلد المشروع الرئيسي
   start scripts/create-user.html
   ```

2. **اختر طريقة الإنشاء:**
   
   **أ. إنشاء سريع (CEO):**
   - انقر على زر "إنشاء CEO الآن ⚡"
   - سيتم إنشاء مستخدم CEO تلقائيًا مع البيانات:
     - البريد الإلكتروني: `ceo@najd.com`
     - كلمة المرور: `Najd@2024`
     - الدور: المدير التنفيذي (CEO)
   
   **ب. إنشاء مخصص:**
   - املأ النموذج بالبيانات المطلوبة
   - اختر الدور المناسب
   - انقر "إنشاء المستخدم"

3. **احفظ معلومات الدخول** التي ستظهر لك

4. **سجل الدخول:**
   - اذهب إلى: http://localhost:3000/login
   - استخدم البريد الإلكتروني وكلمة المرور

## 🔧 الطريقة الثانية: سكريبت Node.js (متقدم)

### المتطلبات:

- تسجيل الدخول إلى Firebase CLI
- أو تشغيل Firebase Emulators

### الخطوات:

```bash
# تثبيت المكتبات (إذا لم يتم)
cd scripts
npm install

# إنشاء مستخدم CEO سريع
npm run create-ceo

# إنشاء مستخدم مخصص (تفاعلي)
npm run create-user
```

## 👥 الأدوار المتاحة

| الدور | الوصف | الصلاحيات |
|------|-------|----------|
| **CEO** | المدير التنفيذي | الوصول الكامل لجميع الأقسام |
| **Sales** | موظف مبيعات | إنشاء وتعديل الطلبات |
| **Sales Head** | مدير المبيعات | إدارة قسم المبيعات |
| **Design** | مصمم | استلام وتنفيذ طلبات التصميم |
| **Design Head** | مدير التصميم | إدارة قسم التصميم |
| **Printing** | موظف طباعة | تنفيذ طلبات الطباعة |
| **Printing Head** | مدير الطباعة | إدارة قسم الطباعة |
| **Accounting** | محاسب | مراجعة المدفوعات |
| **Accounting Head** | مدير الحسابات | إدارة قسم الحسابات |
| **Dispatch** | موظف إرسال | إدارة الإرسال والتسليم |
| **Dispatch Head** | مدير الإرسال | إدارة قسم الإرسال |

## 📝 معلومات المستخدم التجريبي (CEO)

```
البريد الإلكتروني: ceo@najd.com
كلمة المرور: Najd@2024
الدور: CEO (المدير التنفيذي)
القسم: Management
```

## ⚠️ ملاحظات مهمة

### 1. Firestore Security Rules

تأكد من نشر Firestore Rules قبل إنشاء المستخدمين:

```bash
firebase deploy --only firestore:rules
```

أو في وضع التطوير، استخدم test mode في Firestore Console.

### 2. Firebase Authentication

تأكد من تفعيل Email/Password في Firebase Console:
- اذهب إلى: Firebase Console → Authentication
- Sign-in method → Email/Password
- قم بتفعيله

### 3. الأمان

- لا تشارك معلومات الدخول علنًا
- غيّر كلمة مرور المستخدم التجريبي في الإنتاج
- استخدم كلمات مرور قوية

## 🐛 المشاكل الشائعة

### مشكلة: "البريد الإلكتروني مستخدم بالفعل"

**الحل:** 
- المستخدم موجود بالفعل
- يمكنك تسجيل الدخول مباشرة
- أو استخدم بريد إلكتروني آخر

### مشكلة: "خطأ في الصلاحيات" (permission-denied)

**الحل:**
1. تأكد من نشر Firestore Rules:
   ```bash
   firebase deploy --only firestore:rules
   ```

2. أو ضع Firestore في test mode:
   - Firebase Console → Firestore Database
   - Rules tab
   - استخدم:
   ```
   rules_version = '2';
   service cloud.firestore {
     match /databases/{database}/documents {
       match /{document=**} {
         allow read, write: if true; // للتطوير فقط!
       }
     }
   }
   ```

### مشكلة: "كلمة المرور ضعيفة"

**الحل:**
- كلمة المرور يجب أن تكون 6 أحرف على الأقل
- استخدم كلمة مرور أقوى

## 📚 الملفات

- `create-user.html` - صفحة ويب لإنشاء المستخدمين (موصى به)
- `create-user.js` - سكريبت Node.js لإنشاء المستخدمين
- `package.json` - تعريف السكريبتات والمكتبات

## 🔗 روابط مفيدة

- التطبيق: http://localhost:3000
- صفحة تسجيل الدخول: http://localhost:3000/login
- Firebase Console: https://console.firebase.google.com

---

© 2024 شركة نجد - جميع الحقوق محفوظة


