# ميزة إضافة منتجات متعددة في الطلب ✅

## 🎯 المطلوب

السماح لموظف المبيعات بإضافة **أكثر من منتج** في طلب واحد.

## ✅ التحديثات المطبقة

### 1. بنية البيانات الجديدة

#### قبل (منتج واحد):
```typescript
const [printType, setPrintType] = useState<PrintType>(PrintType.DIGITAL);
const [quantity, setQuantity] = useState<number>(1);
const [needsDesign, setNeedsDesign] = useState(false);
const [designDescription, setDesignDescription] = useState('');
```

#### بعد (منتجات متعددة):
```typescript
interface Product {
  id: string;
  printType: PrintType;
  quantity: number;
  needsDesign: boolean;
  designDescription: string;
  description: string; // وصف المنتج
}

const [products, setProducts] = useState<Product[]>([
  {
    id: Math.random().toString(36).substr(2, 9),
    printType: PrintType.DIGITAL,
    quantity: 1,
    needsDesign: false,
    designDescription: '',
    description: '',
  },
]);
```

### 2. دوال إدارة المنتجات

#### إضافة منتج جديد:
```typescript
const addProduct = () => {
  setProducts([
    ...products,
    {
      id: Math.random().toString(36).substr(2, 9),
      printType: PrintType.DIGITAL,
      quantity: 1,
      needsDesign: false,
      designDescription: '',
      description: '',
    },
  ]);
};
```

#### حذف منتج:
```typescript
const removeProduct = (id: string) => {
  if (products.length === 1) {
    toast.error('يجب أن يحتوي الطلب على منتج واحد على الأقل');
    return;
  }
  setProducts(products.filter((p) => p.id !== id));
};
```

#### تحديث منتج:
```typescript
const updateProduct = (id: string, field: keyof Product, value: any) => {
  const updated = products.map((p) =>
    p.id === id ? { ...p, [field]: value } : p
  );
  setProducts(updated);
};
```

### 3. الواجهة الجديدة

#### قسم المنتجات:

```jsx
{/* المنتجات */}
<section>
  <div className="flex justify-between items-center mb-4">
    <h2 className="text-xl font-bold text-gray-900">المنتجات</h2>
    <button
      type="button"
      onClick={addProduct}
      className="px-4 py-2 bg-najd-gold text-najd-blue rounded-md hover:bg-yellow-500"
    >
      + إضافة منتج
    </button>
  </div>

  <div className="space-y-6">
    {products.map((product, index) => (
      <div key={product.id} className="border rounded-lg p-6 bg-gray-50">
        {/* حقول المنتج */}
      </div>
    ))}
  </div>
</section>
```

#### حقول كل منتج:
- **وصف المنتج** - نص يصف المنتج (مثل: بنر إعلاني، كرت شخصي)
- **نوع الطباعة** - ديجيتال، أوفست، إندور
- **الكمية** - عدد القطع المطلوبة
- **يحتاج تصميم** - checkbox
- **وصف التصميم** - يظهر إذا اختار "يحتاج تصميم"

### 4. حفظ البيانات

#### في Firestore:
```typescript
const orderData = {
  orderNumber,
  status: submitForReview ? OrderStatus.PENDING_CEO_REVIEW : OrderStatus.DRAFT,
  priority,
  customerName,
  customerPhone,
  // حفظ نوع الطباعة والكمية الأساسية للتوافق
  printType: primaryPrintType,
  quantity: totalQuantity,
  needsDesign,
  designDescription: products
    .filter((p) => p.needsDesign && p.designDescription)
    .map((p) => p.designDescription)
    .join(' | '),
  // المنتجات المتعددة
  products: products.map((p) => ({
    id: p.id,
    printType: p.printType,
    quantity: p.quantity,
    needsDesign: p.needsDesign,
    designDescription: p.designDescription,
    description: p.description,
  })),
  materials,
  files: uploadedFiles,
  notes,
  // ... باقي الحقول
};
```

## 🎨 الواجهة الجديدة

### مثال على شكل النموذج:

```
┌─────────────────────────────────────────────┐
│         المنتجات         [+ إضافة منتج]     │
├─────────────────────────────────────────────┤
│ ┌─ منتج #1 ─────────────────────── [× حذف] │
│ │ وصف المنتج: [بنر إعلاني 3×2 متر]        │
│ │ نوع الطباعة: [ديجيتال ▼]                │
│ │ الكمية: [5]                              │
│ │ ☑ يحتاج تصميم                            │
│ │ وصف التصميم: [تصميم مودرن بألوان...]    │
│ └──────────────────────────────────────────┘
│                                              │
│ ┌─ منتج #2 ─────────────────────── [× حذف] │
│ │ وصف المنتج: [كرت شخصي]                  │
│ │ نوع الطباعة: [أوفست ▼]                  │
│ │ الكمية: [1000]                           │
│ │ ☐ يحتاج تصميم                            │
│ └──────────────────────────────────────────┘
│                                              │
│ [+ إضافة منتج]                              │
└─────────────────────────────────────────────┘
```

## ✨ الميزات الجديدة

### 1. إضافة منتجات غير محدودة
- يمكن إضافة أي عدد من المنتجات
- زر "+ إضافة منتج" في أعلى القسم
- كل منتج في بطاقة منفصلة

### 2. حذف المنتجات
- زر "× حذف" في كل بطاقة منتج
- لا يمكن حذف جميع المنتجات (يجب بقاء واحد على الأقل)
- رسالة تحذير إذا حاول المستخدم حذف آخر منتج

### 3. معلومات منفصلة لكل منتج
- كل منتج له نوع طباعة خاص
- كل منتج له كمية خاصة
- كل منتج يمكن أن يحتاج تصميم أو لا
- كل منتج له وصف تصميم منفصل

### 4. حساب الكميات الإجمالية
```typescript
const totalQuantity = products.reduce((sum, p) => sum + p.quantity, 0);
```

### 5. الترقيم التلقائي
- "منتج #1"، "منتج #2"، إلخ
- يتحدث تلقائياً عند الإضافة أو الحذف

## 📊 مثال على البيانات المحفوظة

### طلب يحتوي على 3 منتجات:

```json
{
  "orderNumber": "NAJD-2025-0001",
  "customerName": "أحمد محمد",
  "printType": "digital",      // نوع الطباعة الأساسي
  "quantity": 1505,             // الكمية الإجمالية (5 + 1000 + 500)
  "products": [
    {
      "id": "abc123",
      "description": "بنر إعلاني 3×2 متر",
      "printType": "digital",
      "quantity": 5,
      "needsDesign": true,
      "designDescription": "تصميم مودرن بألوان الشركة"
    },
    {
      "id": "def456",
      "description": "كرت شخصي",
      "printType": "offset",
      "quantity": 1000,
      "needsDesign": false,
      "designDescription": ""
    },
    {
      "id": "ghi789",
      "description": "بروشور A4",
      "printType": "digital",
      "quantity": 500,
      "needsDesign": true,
      "designDescription": "بروشور تعريفي بالخدمات"
    }
  ],
  "materials": [...],
  "files": [...],
  // ... باقي الحقول
}
```

## 🎯 حالات الاستخدام

### 1. طلب بمنتج واحد
```
✓ بنر إعلاني × 5
```

### 2. طلب بمنتجات متعددة
```
✓ بنر إعلاني × 5
✓ كرت شخصي × 1000
✓ بروشور A4 × 500
```

### 3. طلب مع تصميم لبعض المنتجات
```
✓ بنر إعلاني × 5      [يحتاج تصميم]
✓ كرت شخصي × 1000      [لا يحتاج تصميم]
✓ بروشور A4 × 500      [يحتاج تصميم]
```

### 4. طلب بأنواع طباعة مختلفة
```
✓ بنر إعلاني × 5       [ديجيتال]
✓ كرت شخصي × 1000      [أوفست]
✓ بروشور A4 × 500      [ديجيتال]
```

## 🔄 التوافق مع النظام القديم

### الحقول المحفوظة للتوافق:
- `printType` - نوع الطباعة الأول
- `quantity` - مجموع الكميات
- `needsDesign` - هل أي منتج يحتاج تصميم؟
- `designDescription` - جميع وصوفات التصميم مفصولة بـ ` | `

### الحقول الجديدة:
- `products[]` - قائمة المنتجات التفصيلية

هذا يضمن أن:
- ✅ الطلبات القديمة تعمل بدون مشاكل
- ✅ الطلبات الجديدة تحتوي على معلومات تفصيلية
- ✅ لا حاجة لتحديث صفحات العرض القديمة

## 🧪 الاختبار

### الخطوات:

1. **اذهب إلى صفحة إنشاء طلب:**
   ```
   http://localhost:3000/orders/new
   ```

2. **املأ معلومات العميل:**
   - الاسم: أحمد محمد
   - الهاتف: 0501234567

3. **أضف منتج أول:**
   - الوصف: بنر إعلاني
   - نوع الطباعة: ديجيتال
   - الكمية: 5
   - ☑ يحتاج تصميم

4. **انقر "+ إضافة منتج"**

5. **أضف منتج ثاني:**
   - الوصف: كرت شخصي
   - نوع الطباعة: أوفست
   - الكمية: 1000
   - ☐ لا يحتاج تصميم

6. **انقر "+ إضافة منتج"**

7. **أضف منتج ثالث:**
   - الوصف: بروشور
   - نوع الطباعة: ديجيتال
   - الكمية: 500

8. **احذف المنتج الثالث (للاختبار)**
   - انقر "× حذف" بجانب "منتج #3"

9. **أرسل الطلب**

### النتيجة المتوقعة ✅

```
✓ تم إرسال الطلب للمراجعة بنجاح
✓ الطلب يحتوي على منتجين
✓ الكمية الإجمالية: 1005
✓ كل منتج محفوظ بمعلوماته الخاصة
```

## 📱 التصميم المتجاوب

### Desktop:
```
┌──────────────────────────┬──────────────────────────┐
│ وصف المنتج              │ نوع الطباعة              │
├──────────────────────────┼──────────────────────────┤
│ الكمية                  │ ☑ يحتاج تصميم            │
└──────────────────────────┴──────────────────────────┘
```

### Mobile:
```
┌──────────────────────────┐
│ وصف المنتج              │
├──────────────────────────┤
│ نوع الطباعة              │
├──────────────────────────┤
│ الكمية                  │
├──────────────────────────┤
│ ☑ يحتاج تصميم            │
└──────────────────────────┘
```

## ✅ الملخص

| الميزة | الحالة |
|--------|--------|
| إضافة منتجات متعددة | ✅ |
| حذف المنتجات | ✅ |
| تحديث معلومات المنتج | ✅ |
| وصف لكل منتج | ✅ |
| نوع طباعة لكل منتج | ✅ |
| كمية لكل منتج | ✅ |
| تصميم منفصل لكل منتج | ✅ |
| حساب الكمية الإجمالية | ✅ |
| حفظ في Firestore | ✅ |
| تصميم متجاوب | ✅ |

## 🎉 الآن يمكن للمبيعات:

- ✅ إضافة طلب بمنتج واحد أو أكثر
- ✅ كل منتج بمواصفاته الخاصة
- ✅ إدارة المنتجات بسهولة (إضافة/حذف/تعديل)
- ✅ تفاصيل دقيقة لكل منتج
- ✅ طباعة أفضل وأوضح للطلبات

**الميزة جاهزة ومفعلة!** 🚀✨

---

© 2024 شركة نجد - جميع الحقوق محفوظة


